
        * {
            box-sizing: border-box;
        }

        body {
           font-family: 'Inconsolata';
            color: rgb(0, 0, 0);
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            overflow-x: hidden;
        }
        


h3 {
  font-family: 'Inconsolata';
  font-size: 18pt;
  font-weight:lighter;
  margin: 0;
  padding: 0;
  margin-left: 10px;
  margin-top: 10px;
  cursor:default;
}

h4 {
  font-family: 'Inconsolata';
  font-size: 12pt;
  font-weight:lighter;
  margin: 0;
  padding: 0;
  margin-left: 10px;
  margin-top: 5px;
  cursor:default;
}


/* section */
h1 {
  font-family: 'Inconsolata';
  font-size: 12pt;
  font-weight:bold;
  margin: 0;
  padding: 0;
  margin-left: 10px;
  margin-top: 10px;
  cursor: pointer;
}

h2 {
  font-family: 'Inconsolata';
  font-size: 12pt;
  font-weight: normal; /* regular weight */
  margin: 0;
  padding: 0;
  margin-left: 10px;
  cursor: pointer;
}

h1:hover {
  text-decoration: underline;
}

h2:hover {
  text-decoration: underline;
}

 /* Vertical gallery */
  .image-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }


  .image-item {
  display: inline-block; /* or flex, but not position absolute unless needed */
  margin: 10px;
  text-align: left; /* makes caption stay under image */
  margin-left: 20px;
}

.caption {
  font-size: 0.8em;
  align-items: left;
  margin-top: 3px;
}

  .image-item img {
    max-height: 200px;
    cursor: pointer;
  }


.lightbox {
  display: none; /* Hidden by default */
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.377);
  justify-content: center; /* Horizontal centering */
  align-items: center;     /* Vertical centering */
  z-index: 1000;
  overflow: auto;
  display: none; /* Make it a flex container */
}


    .lightbox img,
    .lightbox video {
      max-width: 90%;
      max-height: 80%;
      margin-bottom: 10px;
    }

    
  .lightbox.show {
    display: flex;
    justify-content: center; /* Horizontal centering */
    align-items: center;     /* Vertical centering */
  }

  .lightbox-img {
    max-width: 90%;
    max-height: 80%;
  }

  .nav-prev, .nav-next {
    color: white;
    font-size: 2em;
    cursor: pointer;
    user-select: none;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
  }

  .nav-prev { left: 20px; }
  .nav-next { right: 20px; }

  .close-btn {
    position: absolute;
    top: 20px;
    right: 30px;
    color: white;
    font-size: 2em;
    cursor: pointer;
  }



  .accordion {
    cursor:default;
    color: #000000;
    padding: 5px;
    font-size: 16px;
    border: none;
    text-align: left;
    align-items: left;
    background: none;
    outline: none;
    display: block;
    width: 100%;
    text-decoration: none; /* Ensure no underline by default */
    margin-left: 10px;
}

.accordion:hover {
    text-decoration: underline; /* Add underline on hover */
}


/* Remove default arrow */
details.no-arrow > summary {
  list-style: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  margin: 0;           /* remove default margin */
  padding: 0;          /* optional, normalize padding */
}



/* Normalize headers inside summary */
details summary h1,
details summary h2,
details summary h3 {
  margin: 0;
  padding: 0;
  display: inline; /* prevent block behavior */
  font-weight: normal;
}



/* Ensure nested content inside summary doesn't stretch */
details summary > div,
details summary > h1,
details summary > h2,
details summary > h3 {
  display: inline-block;
  cursor: pointer;           /* pointer only on the text elements */
}

/* Optional: style nested details */
details details {
  margin-left: 20px;
}

/* Optional: prevent all <details> from full width */
details {
  width: fit-content;
  max-width: 100%;
}




/* Inner gallery summary style */
.text-style {
  font-family: 'Times New Roman', Times, serif;
  font-size: 12px;
  margin: 0;
  padding-left: 20px;  /* consistent indentation */
}


.no-arrow summary {
  cursor: pointer;        /* make it clickable */
}





@media (max-width: 768px) {
    details summary::-webkit-details-marker {
        display: none; /* Hide the dropdown arrows */
    }
}

        

 label {
            cursor: pointer;
        }
